pipeline {
    agent any

        tools {
            maven "maven_j"
        }

    stages {
        stage('Deploy') {
            steps {
                echo 'Deploying....'
               sh 'mvn clean deploy -P master'
            }
        }
    }
}